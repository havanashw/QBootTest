% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/QBootTest.R
\name{QBootTest}
\alias{QBootTest}
\title{Multiplier bootstrap tests for high-dimensional quantile regression}
\usage{
QBootTest(
  x,
  y,
  tau = 0.5,
  method = c("asymptotic", "bootstrap"),
  multiplier = c("gaussian", "rademacher"),
  M = 500,
  scale = FALSE,
  return_details = FALSE
)
}
\arguments{
\item{x}{Numeric matrix of predictors with dimension n x p.}

\item{y}{Numeric response vector of length n.}

\item{tau}{Quantile level in (0, 1). Default is 0.5.}

\item{method}{Either "asymptotic" or "bootstrap".}

\item{multiplier}{Either "gaussian" or "rademacher". Used when method = "bootstrap".}

\item{M}{Number of bootstrap replicates. Used when method = "bootstrap".}

\item{scale}{Logical; if TRUE, standardize x and y.}

\item{return_details}{Logical; if TRUE, return a list with statistic and settings.}
}
\value{
If return_details = FALSE (default), returns a single p-value.
If TRUE, returns a list with elements pval, Tn, Tn_std (if asymptotic),
and bootstrap_stats (if bootstrap).
}
\description{
Multiplier bootstrap tests for high-dimensional quantile regression
}
\examples{
set.seed(1)
n <- 80; p <- 50
x <- matrix(rnorm(n * p), n, p)
y <- rnorm(n)
quantile_score_test(x, y, tau = 0.5, method = "asymptotic")
quantile_score_test(x, y, tau = 0.5, method = "bootstrap", M = 200)

}
